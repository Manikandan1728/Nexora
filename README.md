# AI Personal Knowledge Engine
A multimodal semantic search engine that processes WhatsApp exports (ZIP or extracted folders), parses chats and media, generates embeddings, stores them in a vector database, and answers user queries using Retrieval-Augmented Generation (RAG).
